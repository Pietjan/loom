name: site

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v6
        with:
          go-version-file: site/go.mod
      - name: Install tailwindcss standalone
        run: |
          curl -sSL -o /usr/local/bin/tailwindcss \
            https://github.com/tailwindlabs/tailwindcss/releases/download/v4.3.3/tailwindcss-linux-x64
          chmod +x /usr/local/bin/tailwindcss
      # *_templ.go is generated output and is not committed, so the pages
      # package does not compile until the runner generates it. The templ CLI
      # is pinned by the tools module; -modfile runs it from there.
      - name: Generate templ output
        run: go tool -modfile=tools/go.mod templ generate -path site
      # The generated Tailwind entry file embeds an absolute path to the loom
      # module, so CSS must be rebuilt on the runner rather than committed.
      - name: Build CSS
        working-directory: site
        run: |
          go run github.com/pietjan/loom/cmd/css -o css/input.css
          cat css/site.css >> css/input.css
          tailwindcss -i css/input.css -o static/styles.css --minify
      - name: Render static site
        working-directory: site
        run: go run . build -o dist -base /loom/
      # include-hidden-files keeps the .nojekyll that `build` writes into dist;
      # v4 of this action started excluding dotfiles by default.
      - uses: actions/upload-pages-artifact@v5
        with:
          path: site/dist
          include-hidden-files: true

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
